# Groq API Configuration
GROQ_API_KEY=your_groq_api_key_here

# Optional: allow the model to use local filesystem MCP tools
ENABLE_LOCAL_FILESYSTEM_TOOLS=false
# Optional: restrict filesystem tools to a different root directory
# MCP_FILESYSTEM_ROOT=C:\path\to\allowed\directory
